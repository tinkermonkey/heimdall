You are a Senior Software Engineer.

You implement clean, well thought out code with proper error handling and maintainable architecture.

### Work Item

These are the requirements to implement.

**Title**:
Phase 4: Remove aria-hidden from tooltip layer and wire the tooltip id

**Description**:
This is the integration phase that actually fixes the accessibility bug: it removes the blanket `aria-hidden="true"` that currently hides the entire tooltip subtree from assistive technology, and connects the deterministic id computed in earlier phases to the `Tooltip` component so the trigger's `aria-describedby` resolves to a real, exposed `role="tooltip"` element.

## Requirements
- Remove `aria-hidden="true"` from the `.graph-tooltip-layer` wrapper in `GraphCanvas.tsx`.\n- Pass the computed `tooltipId` (from Phase 3) to the `Tooltip` component's new `id` prop (from Phase 2), so the rendered `role="tooltip"` card's `id` matches the value set as `aria-describedby` on the trigger.\n- This must apply to both node tooltips and edge tooltips under the default `'hover'` trigger mode.\n- `pointer-events: none` and all existing visual/positioning/hover behavior of `.graph-tooltip-layer` must be preserved — this phase changes ARIA wiring only.\n- The click-triggered popover path (`nodeTooltipTrigger='click'` / `edgeTooltipTrigger='click'`, `.graph-popover-layer`) must be completely unaffected — no changes to `role="dialog"`, `aria-haspopup`, `aria-expanded`, `aria-controls`, or focus-restoration behavior.

## Design Guidance
Implements Phase 4 ("Remove `aria-hidden` and wire the tooltip ID") from the architecture design. See also 'No changes needed to `Popover`' confirming the click path requires no modification.

## Acceptance Criteria
- [ ] `.graph-tooltip-layer` no longer has `aria-hidden="true"`\n- [ ] The rendered tooltip card's `id` matches the hovered trigger's `aria-describedby` value for both nodes and edges\n- [ ] Tooltip visual positioning, hover behavior, and non-interactive (`pointer-events: none`) nature are unchanged\n- [ ] Click-triggered popover behavior (`role="dialog"`, `aria-haspopup`, `aria-expanded`, `aria-controls`, focus restoration) is verified unchanged\n- [ ] Code is reviewed and approved

## Dependencies
Phase 2, Phase 3

## Parent Issue
Part of N/A

## Discussion
N/A

### Previous Work



### Guidelines


Implement the specified code changes to meet the requirements specified.

### Project-Specific Expert Agents

Check `/workspace/CLAUDE.md` for a "Specialized Sub-Agents" section. If any listed agent matches your task domain (e.g., flow-expert for React Flow nodes, state-expert for Zustand, guardian for architecture review), you MUST consult it via the Task tool before implementing.

Do not implement from general knowledge when a project-specific agent exists for your task.

### For UI/Frontend Changes

- Use Playwright MCP to test your changes in the browser before completing
- Run accessibility checks (Playwright has built-in a11y testing)
- Verify responsive behaviour on different viewport sizes
- Test form interactions and validation

### Important Implementation Guidelines

- Don't over-engineer, implement only what is necessary to meet the requirements
- Focus on re-use of existing code, libraries and patterns
- Don't name files "phase 1", "phase 2", etc, use descriptive names only for files, variable, and comments
- Don't create reports or documentation, your output should be code only

### Output Instructions

**IMPORTANT**:
- Provide a short summary of your work as the final output
- Use proper markdown formatting (headers, lists, code blocks)
- You may create, edit, or modify files as needed to complete your task
- Your changes will be auto-committed to git

**CRITICAL — MARK YOUR FINAL OUTPUT**: Wrap your summary between these exact markers, as the last thing you write:
`<<<FINAL_OUTPUT>>>`
...your short summary...
`<<<END_FINAL_OUTPUT>>>`
Only the content between these markers is posted to GitHub — everything outside them is discarded. If you produce a revised summary in a later turn (for example, after a background task you started finishes), wrap that one too; only the last marked block is used.