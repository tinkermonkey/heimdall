You are a Senior Software Engineer.

You implement clean, well thought out code with proper error handling and maintainable architecture.

### Work Item

These are the requirements to implement.

**Title**:
Phase 6: QuickAccessTile, ConfigTile, FilterBar children slot

**Description**:
Extract `QuickAccessTile` from `QuickAccessGrid` internals as a standalone component, add `ConfigTile` extending the tile pattern with a kv-mini strip, and extend `FilterBar` with a `children` slot so consumers can place `FilterDropdown` and `SegmentedControl` inline with the search input.

## Requirements
- `QuickAccessTile` accepts `icon: IconName`, `title: string`, `description: string`, `onClick: () => void`; renders a flat link-card with icon, title, description, chevron; no internal state
- `QuickAccessGrid` updated to render `QuickAccessTile` instances internally (existing grid API preserved)
- `ConfigTile` accepts `icon: IconName`, `title: string`, `description: string`, `summary: { label: string, value: string }[]`, `onClick: () => void`; adds a kv-mini strip in mono 11px below description
- `FilterBar` gains a `children` prop; existing search-input and chip-removal behaviour unchanged; `showingCount`/`totalCount` caption remains on the right; row layout accommodates `FilterDropdown` and `SegmentedControl` children inline
- All three new/extended components exported from `src/index.ts` with prop types

## Design Guidance
Implements the Phase 6 tile components and FilterBar extension from the architecture design. Covers FR-10 (QuickAccessTile), FR-11 (ConfigTile), FR-13 (FilterBar children slot), and the QuickAccessTile extraction recommendation from the Component Reuse section.

## Acceptance Criteria
- [ ] `QuickAccessTile` renders icon, title, description, chevron in a flat card; `onClick` fires on click
- [ ] `QuickAccessGrid` continues to render correctly using `QuickAccessTile` internally
- [ ] `ConfigTile` renders the kv-mini strip with mono 11px labels and values below description
- [ ] `FilterBar` with `FilterDropdown` and `SegmentedControl` as children shows all controls on a single row alongside the search input
- [ ] `FilterBar` with no children behaves identically to current behaviour
- [ ] "showing N of M" caption appears on the right when `showingCount`/`totalCount` are set
- [ ] All components and prop types exported from `src/index.ts`

## Dependencies
Phase 1, Phase 5


### Previous Work



### Guidelines


Implement the specified code changes to meet the requirements specified.

### Project-Specific Expert Agents

Check `/workspace/CLAUDE.md` for a "Specialized Sub-Agents" section. If any listed agent matches your task domain (e.g., flow-expert for React Flow nodes, state-expert for Zustand, guardian for architecture review), you MUST consult it via the Task tool before implementing.

Do not implement from general knowledge when a project-specific agent exists for your task.

### For UI/Frontend Changes

- Use Playwright MCP to test your changes in the browser before completing
- Run accessibility checks (Playwright has built-in a11y testing)
- Verify responsive behaviour on different viewport sizes
- Test form interactions and validation

### Important Implementation Guidelines

- Don't over-engineer, implement only what is necessary to meet the requirements
- Focus on re-use of existing code, libraries and patterns
- Don't name files "phase 1", "phase 2", etc, use descriptive names only for files, variable, and comments
- Don't create reports or documentation, your output should be code only

### Output Instructions

**IMPORTANT**:
- Provide a short summary of your work as the final output
- Use proper markdown formatting (headers, lists, code blocks)
- You may create, edit, or modify files as needed to complete your task
- Your changes will be auto-committed to git