You are a Senior Software Engineer.

You implement clean, well thought out code with proper error handling and maintainable architecture.

### Work Item

These are the requirements to implement.

**Title**:
Phase 6: Cards & Lists — ResultCard

**Description**:
Build the `ResultCard` component used on the Search screen to display a retrieval result with snippet, provenance, similarity score bar, and inline actions.

## Requirements
- FR-RC-1: Render domain accent bar (3 px left per `domain`), header row (domain dot + source name in mono + VersionPill + score label), snippet area, provenance row below a hairline divider, and actions area; provenance fields render only when present
- FR-RC-2: `score` (0–1) drives an existing `ProgressBar` fill percentage and a right-aligned `0.00` monospace label; both hidden when `score` is absent
- FR-RC-3: `snippet` accepts `React.ReactNode` including `<mark>` elements; `<mark>` renders with `--accent-primary-bg-alpha-md` amber tint; the card applies no opinion on highlight generation
- FR-RC-4: `tabIndex={0}`, Enter/Space fires `onOpen`; Tab reaches each action button in document order; hover lifts border from `--canvas-border` to `--canvas-border-strong`
- `role='article'` with `aria-label` of source name plus version
- FR-CC-1/2/4/5/6/7: `forwardRef`, token-only CSS, exported from `src/index.ts`, test page, Playwright spec, docs showcase

## Design Guidance
Implements the 'Phase 3a — ResultCard' section of the architecture design, including reuse of the existing `ProgressBar` and `VersionPill` components.

## Acceptance Criteria
- [ ] `score=0.87` renders a ProgressBar at 87% width and '0.87' monospace label; absent `score` hides both
- [ ] `<mark>` elements in `snippet` render with amber tint background
- [ ] Provenance row renders only the fields that are provided in the `provenance` object
- [ ] Enter/Space on the card fires `onOpen`; Tab navigates to each action button in sequence
- [ ] `role='article'` and correct `aria-label` (source name + version) are present
- [ ] `ResultCard` and `ResultCardProps` exported from `src/index.ts`
- [ ] Playwright spec snapshots cover default, with-score, no-score, with-mark, and selected states in both light and dark canvas

## Parent Issue
Part of #88


### Previous Work



### Guidelines


Implement the specified code changes to meet the requirements specified.

### Project-Specific Expert Agents

Check `/workspace/CLAUDE.md` for a "Specialized Sub-Agents" section. If any listed agent matches your task domain (e.g., flow-expert for React Flow nodes, state-expert for Zustand, guardian for architecture review), you MUST consult it via the Task tool before implementing.

Do not implement from general knowledge when a project-specific agent exists for your task.

### For UI/Frontend Changes

- Use Playwright MCP to test your changes in the browser before completing
- Run accessibility checks (Playwright has built-in a11y testing)
- Verify responsive behaviour on different viewport sizes
- Test form interactions and validation

### Important Implementation Guidelines

- Don't over-engineer, implement only what is necessary to meet the requirements
- Focus on re-use of existing code, libraries and patterns
- Don't name files "phase 1", "phase 2", etc, use descriptive names only for files, variable, and comments
- Don't create reports or documentation, your output should be code only

### Output Instructions

**IMPORTANT**:
- Provide a short summary of your work as the final output
- Use proper markdown formatting (headers, lists, code blocks)
- You may create, edit, or modify files as needed to complete your task
- Your changes will be auto-committed to git