You are a Senior Software Engineer.

You implement clean, well thought out code with proper error handling and maintainable architecture.

### Work Item

These are the requirements to implement.

**Title**:
Phase 12: Integration & final validation

**Description**:
Audit barrel exports, wire all new components into the bare demo app and docs/ showcase app, and run a full Playwright regression pass to confirm all new and pre-existing baselines are green.

## Requirements
- FR-CC-4: Audit `src/index.ts` — every new component, sub-component, hook, and TypeScript type must be exported in the correct alphabetical-by-category group; no missing or duplicate entries
- FR-CC-5: Each new component must have a dedicated test page in `src/test-pages/` reachable via `?test=` or `?example=` query params in the bare app
- FR-CC-6: Run `npm test` and confirm all new Playwright specs pass and all pre-existing specs (especially GraphCanvas and ActivityTimeline) remain green
- FR-CC-7: Each new component is represented in the bare demo app with an example rendering its significant states
- FR-CC-8: Each new component has a showcase page in `docs/src/showcases/` using `PageHeader`, `ShowcaseSection`, `DemoRow`/`DemoGrid`, and `PropsTable`; all showcase pages are wired into `docs/src/App.tsx` sidebar navigation
- Confirm no hardcoded hex values exist in any new component `.css` file
- Confirm no residual inline pan/zoom logic remains in `GraphCanvas.tsx`
- Confirm `src/utils/dateUtils.ts` is the sole location of time-formatting and date-math utilities with no duplicate implementations in component files

## Design Guidance
Implements the 'Phase 5 — Integration & Validation' section of the architecture design covering test pages, Playwright specs, docs showcases, and barrel export audit.

## Acceptance Criteria
- [ ] `src/index.ts` exports all new components, sub-components, the `usePanZoom` hook, and all public TypeScript types with no missing or duplicate entries
- [ ] Every new component has a test page reachable from the bare app and renders all documented states
- [ ] `npm test` exits green with zero snapshot failures across all specs, new and pre-existing
- [ ] Each new component appears in the bare demo app with its significant states rendered
- [ ] Each new component has a docs showcase page with `PageHeader`, `ShowcaseSection`, `DemoRow`/`DemoGrid`, and `PropsTable`
- [ ] All showcase pages appear in the `docs/src/App.tsx` sidebar navigation
- [ ] `grep` over all new `.css` files finds zero hardcoded hex color values
- [ ] `grep` over `GraphCanvas.tsx` finds no residual inline pan/zoom state refs or wheel/pointer handlers
- [ ] `src/utils/dateUtils.ts` is the sole location of time-formatting and date-math utilities; no duplicate implementations exist in component files

## Dependencies
Phase 0, Phase 1, Phase 2, Phase 3, Phase 4, Phase 5, Phase 6, Phase 7, Phase 8, Phase 9, Phase 10, Phase 11

## Parent Issue
Part of #88


### Previous Work



### Guidelines


Implement the specified code changes to meet the requirements specified.

### Project-Specific Expert Agents

Check `/workspace/CLAUDE.md` for a "Specialized Sub-Agents" section. If any listed agent matches your task domain (e.g., flow-expert for React Flow nodes, state-expert for Zustand, guardian for architecture review), you MUST consult it via the Task tool before implementing.

Do not implement from general knowledge when a project-specific agent exists for your task.

### For UI/Frontend Changes

- Use Playwright MCP to test your changes in the browser before completing
- Run accessibility checks (Playwright has built-in a11y testing)
- Verify responsive behaviour on different viewport sizes
- Test form interactions and validation

### Important Implementation Guidelines

- Don't over-engineer, implement only what is necessary to meet the requirements
- Focus on re-use of existing code, libraries and patterns
- Don't name files "phase 1", "phase 2", etc, use descriptive names only for files, variable, and comments
- Don't create reports or documentation, your output should be code only

### Output Instructions

**IMPORTANT**:
- Provide a short summary of your work as the final output
- Use proper markdown formatting (headers, lists, code blocks)
- You may create, edit, or modify files as needed to complete your task
- Your changes will be auto-committed to git