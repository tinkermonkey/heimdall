You are a Senior Software Engineer.

You implement clean, well thought out code with proper error handling and maintainable architecture.

### Work Item

These are the requirements to implement.

**Title**:
Phase 3: Chat UX Components

**Description**:
Implement the full Chat UX component set: ChatMessage, ToolBlock, ChatDivider, ChatSuggestions, ChatComposer, and ChatContainer. Replace all mocks in ChatShowcase.tsx with real @heimdall/ui exports. This is the densest showcase page and requires interaction tests in addition to visual snapshots.

## Requirements
- FR-1.1/1.2: ChatMessage renders sender avatar (initials or icon), sender name, timestamp, and body; user and bot messages have distinct layout and background treatment — no color fills conflicting with the canvas surface
- FR-1.3: ToolBlock is a collapsible panel showing tool name, status indicator (amber=running, emerald=success, rose=error), and key-value output rows; collapsed state shows only name and status
- FR-1.4: ChatComposer contains a multi-line text input, scope/context indicator, removable context-pill slots, a send action, and keyboard shortcut hints; Enter (without Shift) submits and clears input; Shift+Enter inserts a newline without submitting
- FR-1.5: ChatSuggestions renders a horizontal row of clickable pills; calls onSelect with the suggestion text; renders nothing when suggestions is an empty array
- FR-1.6: ChatDivider renders a labeled horizontal rule for date or session boundaries
- FR-1.7/1.8: ChatContainer composes a scrollable message thread, an optional bot-tab strip with status dots and role labels, and a ChatComposer anchored at the bottom; tab switching is controlled via onBotChange prop/callback
- FR-1.9: All chat components render correctly in both light-canvas and dark-canvas (body.dark-canvas) modes using CSS variable overrides — no filter() or invert()
- FR-6: Add send, bot, and paperclip icons to the ICONS map if not already present, conforming to the icon spec

## Design Guidance
Implements the Chat phase validation gate described in the architecture design. ChatShowcase.tsx is identified as the densest showcase page — it covers message variants, tool block states (running/complete/error), the composer with attachment slot, suggestion pills, and dividers.

## Acceptance Criteria
- [ ] ChatMessage renders avatar, sender name, timestamp, and body for both user and bot roles
- [ ] Bot and user messages have visually distinct layout and background treatment
- [ ] ChatMessage with toolBlock prop renders a ToolBlock sub-component below the message body
- [ ] ToolBlock collapsed state shows only name and status indicator
- [ ] ToolBlock expanded state reveals key-value output rows
- [ ] ToolBlock status indicator uses amber (running), emerald (success), rose (error)
- [ ] ChatComposer Enter submits (calls onSubmit, clears input); Shift+Enter inserts a newline without submitting
- [ ] ChatComposer contextItems prop renders as removable pills; onContextChange fires with updated items on removal
- [ ] ChatComposer scopeLabel prop renders as a non-removable indicator
- [ ] ChatSuggestions renders clickable pills and calls onSelect with suggestion text on click
- [ ] ChatSuggestions renders nothing when suggestions prop is an empty array
- [ ] ChatDivider renders a labeled horizontal rule
- [ ] ChatContainer renders bot-tab strip with status dots; onBotChange fires with selected bot ID on tab click
- [ ] ChatContainer scrolls to the bottom when new messages are added
- [ ] All chat components pass visual checks in both light-canvas and dark-canvas modes
- [ ] ChatShowcase.tsx uses real @heimdall/ui components with no remaining Mock* components
- [ ] data-testid attributes on ChatMessage, ToolBlock, and ChatComposer
- [ ] Playwright spec covers all sub-component variants, keyboard submit interaction, suggestion click interaction, and both canvas modes


### Previous Work



### Guidelines


Implement the specified code changes to meet the requirements specified.

### Project-Specific Expert Agents

Check `/workspace/CLAUDE.md` for a "Specialized Sub-Agents" section. If any listed agent matches your task domain (e.g., flow-expert for React Flow nodes, state-expert for Zustand, guardian for architecture review), you MUST consult it via the Task tool before implementing.

Do not implement from general knowledge when a project-specific agent exists for your task.

### For UI/Frontend Changes

- Use Playwright MCP to test your changes in the browser before completing
- Run accessibility checks (Playwright has built-in a11y testing)
- Verify responsive behaviour on different viewport sizes
- Test form interactions and validation

### Important Implementation Guidelines

- Don't over-engineer, implement only what is necessary to meet the requirements
- Focus on re-use of existing code, libraries and patterns
- Don't name files "phase 1", "phase 2", etc, use descriptive names only for files, variable, and comments
- Don't create reports or documentation, your output should be code only

### Output Instructions

**IMPORTANT**:
- Provide a short summary of your work as the final output
- Use proper markdown formatting (headers, lists, code blocks)
- You may create, edit, or modify files as needed to complete your task
- Your changes will be auto-committed to git