Your goal is to fix ALL failing integration tests in this project. Every failure listed below must pass before you are done.

Analysis has identified the likely root cause:
The node hierarchy/forest computation is including 4 additional nodes (14 instead of 10 expected descendants) when collapsing. This affects all Galaxy View layout tests. Root cause: The refactoring removed the collapseControl useMemo which was computing visible node metadata; this may have affected dependency tracking for hierarchy updates. The forest or structuralDescendants computation needs verification to ensure it's correctly filtering visible nodes and not including extra descendants in the hierarchy tree.

## Current failure state

Total failures: 3 across 1 files

## Error types (by frequency)
  integration: Graph Canvas Components › Galaxy View - Layout & Relations › renders every node with no overlapping bounding boxes: 1 occurrences
    e.g. tests/graph-components.spec.ts: Test failed - assertion error during graph layout validation
  integration: Graph Canvas Components › Galaxy View - Layout & Relations › collapsing a node hides its structural descendants and shows a hidden-count badge: 1 occurrences
    e.g. tests/graph-components.spec.ts: Test failed - assertion error during node collapse behavior validation
  integration: Graph Canvas Components › Galaxy View - Layout & Relations › expanding a collapsed node restores its structural descendants: 1 occurrences
    e.g. tests/graph-components.spec.ts: Test failed - assertion error during node expand behavior validation

## Most affected files (top 1 of 1)
  tests/graph-components.spec.ts: 3 failures (integration: Graph Canvas Components › Galaxy View - Layout & Relations › renders every node with no overlapping bounding boxes×1, integration: Graph Canvas Components › Galaxy View - Layout & Relations › collapsing a node hides its structural descendants and shows a hidden-count badge×1, integration: Graph Canvas Components › Galaxy View - Layout & Relations › expanding a collapsed node restores its structural descendants×1)



This is attempt 2/3. Focus on the remaining failures shown above.

## How to approach this

1. Examine representative failing files to understand the root cause. The failure state above shows a sample — the full set of affected files may be larger.
2. Use grep or glob to discover every file in the project that exhibits the same pattern, not just the ones listed above.
3. Apply fixes comprehensively across all affected files. Prefer bulk approaches (scripted edits, sed, ast-based transforms) over editing files one at a time.
4. After fixing, run the integration checks to measure progress. Repeat until all failures are resolved.